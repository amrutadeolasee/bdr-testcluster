Compile and install the bdr extension into a PostgreSQL install.

Doesn't `CREATE EXTENSION` or do the BDR node join. This just
ensures the extension is present.

tags:

* bdr_ext_install: install the extension
